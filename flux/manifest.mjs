/**
 * Flux theme manifest — production-grade fixed-sidebar documentation theme.
 * Based on the Feldera docs build (advanced Forge variant).
 */

const manifest = {
	name: "flux",
	version: "2.0.0",
	displayName: "Flux",
	tagline: "Production-grade documentation with fixed sidebar",
	defaults: {
		primaryHue: 290,
		defaultTheme: "system",
		fontFamily: "inter",
	},
};

export default manifest;
